services:
  postgres:
    image: postgres:16-alpine
    container_name: pricetracker_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: qwerty
      POSTGRES_DB: pricetracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_price:/var/lib/postgresql/data
      - ./init_db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: pricetracker_rmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    restart: unless-stopped

  api:
    container_name: price_api
    build:
      context: ./api
      dockerfile: ./Dockerfile
    ports:
      - "9090:8080"
    environment:
      - DB_DSN=postgres://admin:qwerty@postgres:5432/pricetracker
      - RMQ_URL=amqp://guest:guest@rabbitmq:5672/  
    depends_on:
      - postgres
      - rabbitmq
    restart: unless-stopped

  worker:
    container_name: price_worker
    build:
      context: ./worker
      dockerfile: ./Dockerfile
      args:
        app_name: price_tracker
    environment:
      - DB_DSN=postgres://admin:qwerty@postgres:5432/pricetracker
      - RMQ_URL=amqp://guest:guest@rabbitmq:5672/
    depends_on:
      - postgres
      - rabbitmq
    restart: unless-stopped

volumes:
  postgres_price:
